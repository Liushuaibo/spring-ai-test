第一条：Java代码中的变量命名必须严格使用小驼峰命名法，严禁使用拼音与英文混合命名。
第二条：所有的数据库查询禁止使用 SELECT *，必须明确指定需要的字段，以防止全表扫描带来的性能问题。
第三条：微服务之间的调用必须配置合理的超时时间和重试机制，防止雪崩效应。
第四条：严禁在代码中硬编码任何密码、密钥等敏感信息，必须统一从配置中心获取。
第五条：所有的 Controller 接口必须进行统一的参数校验，防止非法数据进入业务逻辑层。